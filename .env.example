# Railway environment variables for Flame
# Railway auto-injects PORT — do NOT set it here (it overrides on each deploy).

# Password for the Flame admin panel (required to save settings / pin apps).
# If unset, Flame defaults to "flame_password".
PASSWORD=change_me

# Optional: bind a specific port (Railway injects PORT; usually best to leave unset).
# PORT=5005
